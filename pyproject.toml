[project]
name = "smart-inbox-env"
version = "0.1.0"
description = "AI Email Triage OpenEnv Environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "openai",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"